<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title><%= titulo %></title>
    
    <link rel="stylesheet" href="/css/style.css"> 
    
</head>
<body>
    <h1><%= titulo %></h1>
    <p>Esta lista se lee de <code>data/productos.json</code> y es persistente.</p>
    
    <% if (listaProductos && listaProductos.length > 0) { %>
        <ul>
            <% listaProductos.forEach(function(producto) { %>
                <li>
                    <strong><%= producto.nombre %></strong> (ID: <%= producto.id %>) - $<%= producto.precio.toFixed(2) %>
                    <p><%= producto.descripcion %></p>
                </li>
            <% }); %>
        </ul>
    <% } else { %>
        <p>No hay productos guardados. <a ***id="enlace-agregar-producto"*** href="/agregar-producto">¡Agrega uno!</a></p>
    <% } %>

    <p><a ***id="enlace-volver-inicio"*** href="/">Volver al inicio</a></p>
</body>
</html>